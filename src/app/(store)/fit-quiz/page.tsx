import { FitQuiz } from "@/components/storefront/FitQuiz";

export const metadata = { title: "Find Your Fit — A&I", description: "A quick, non-intrusive fit quiz to find your size across the A&I collection." };

export default function FitQuizPage() {
  return <FitQuiz />;
}
